import React from "react";

import Scroll from "../Scroll/Scroll";
import { scrollIDs } from "./DataOne";

const ScrollComponent = ({}) => {
  return <Scroll scrollIDs={scrollIDs} />;
};

export default ScrollComponent;
